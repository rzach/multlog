texName(t, "\\mathbf{1}").
texName(u, "\\mathbf{\\sfrac{1}{2}}").
texName(f, "\\mathbf{0}").
texName(forall, \\forall).
texName(exists, \\exists).
texName(sforall, \\bigotimes).
texName(sexists, \\bigoplus).
texName(and,    \\land  ).
texName(or,     \\lor   ).
texName(sand,    \\otimes  ).
texName(sor,     \\oplus   ).
texName(neg,    \\lnot  ).
texName(imp,    \\to).
texInfix(and).
texInfix(or).
texInfix(sand).
texInfix(sor).
texInfix(imp).
texPrefix(neg).

texExtra("Preamble", "\\usepackage{utopia,xfrac}").
texExtra("ShortName", "\\textbf{\\L}_3").
texExtra("Link", "https://logic.at/multlog/lukasiewicz.pdf").