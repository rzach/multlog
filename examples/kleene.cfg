
texName(and,    \\land  ).
texName(or,     \\lor   ).
texName(neg,    \\lnot  ).
texName(imp,    \\to).
texInfix(and).
texInfix(or).
texInfix(imp).
texPrefix(neg).

texExtra("ShortName", "\\mathbf{K}_3").
texExtra("Link", "https://logic.at/multlog/kleene.pdf").