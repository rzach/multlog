% LaTeX configuration file for Goedel logic

texName(forall, \\forall).
texName(exists, \\exists).
texName(and,    \\land  ).
texName(or,     \\lor   ).
texName(neg,    \\lnot  ).
texName(imp,    \\supset).
texName(false,  \\bot).
texName(true,   \\top).
texInfix(and).
texInfix(or).
texInfix(imp).
texPrefix(neg).

texExtra("ShortName", "\\mathbf{G}_3").
texExtra("Link", "https://logic.at/multlog/goedel.pdf").
texExtra("Preamble","\\ESequentstrue").