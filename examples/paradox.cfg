
texName(and,    \\land  ).
texName(or,     \\lor   ).
texName(neg,    \\lnot  ).
texName(imp,    \\to).
texName(equiv,    \\leftrightarrow).
texName(forall,    \\forall).
texName(exists,    \\exists).

texInfix(and).
texInfix(or).
texInfix(imp).
texInfix(equiv).
texPrefix(neg).

texExtra("FullNameOfLogic", "Priest's logic of paradox").
texExtra("ShortName", "\\mathbf{LP}").
texExtra("Link", "https://logic.at/multlog/paradox.pdf").
texExtra("Preamble","\\ESequentstrue\\renewcommand{\\esequent}[3]{##1 \\Rightarrow ##2 \\mid ##3}").